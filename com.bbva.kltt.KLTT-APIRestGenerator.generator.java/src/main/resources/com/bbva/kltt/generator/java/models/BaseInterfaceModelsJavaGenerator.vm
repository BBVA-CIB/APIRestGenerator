/*******************************************
 This class has been automatically generated using KLTT-APIRestGenerator project, don't do manual file modifications.
 ${now}

 "Licensed to the Apache Software Foundation (ASF) under one or more contributor license agreements;
  and to You under the Apache License, Version 2.0. "
********************************************/

package ${packageName} ;

/**
 * Interface that defines the methods to be implemented by the models.
 */
public interface ${className}
{
	/** Clear all the message contents */
	void clear() ;
	
	/**
	 * Randomly fills the object with data, it can be used for testing purposes
	 * @param maxArraySize max size for the internal arrays
	 * @param clear true if the object should be cleared
	 * @param currentRecursiveCall current value of recursive calls, use 0 for the first call in the message
	 * @param maxRecursiveCalls maximum number of recursive calls to prevent infinite loops
	 */
	void fillRandomly(int maxArraySize, boolean clear, int currentRecursiveCall, int maxRecursiveCalls) ;
}
